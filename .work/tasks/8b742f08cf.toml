id = "8b742f08cf"
goal = "README documents every shipped loom command, exit code, environment variable, and policy field, with no stale or missing entries against the 2.0.0 source"
state = "open"
value = 3.0
deps = []
contract = []
accept = []
context = ["Audit README.md (220 lines) against src/. Existing sections: Why, Install, Quickstart, Using loom with an agent, Claude Code plugin, Commands, Exit codes, Environment, Policy, How coordination works, Development, Pre-commit hook. Check each documented item still exists and each shipped item is documented."]
tier_initial = 0
created = "2026-07-29T10:11:35.956868Z"
